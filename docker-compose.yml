services:
  cgm:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"   # localhost only; put a TLS proxy in front
    environment:
      CGM_TOKEN: "${CGM_TOKEN:-}"
      CGM_TZ_OFFSET: "${CGM_TZ_OFFSET:-7}"
